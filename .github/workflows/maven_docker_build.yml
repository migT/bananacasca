name: Build and publish a Docker image
on:
  push:
    branches:
      - '*'
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
jobs:
  build:
    name: Build & push docker image
    runs-on: ubuntu-latest
    env:
      IMG_NAME: josemiguelcamachotavares587/dockerworkflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print triggering branch
        run: |
          echo "github.ref -> ${{ github.ref }}"

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          maven: '3.8.8'

      - name: Define MAVEN_HOME
        run: |
          export MAVEN_HOME=/usr/share/apache-maven-3.8.8
          echo MAVEN_HOME= $MAVEN_HOME

      - name: Print Maven version
        run: |
          printenv

      - name: Build with Maven
        run: mvn clean package

      - name: Run app
        run: |
          export MY_ENV_VAR=$(find ./ -maxdepth 1 -type d -name 'apache-maven-*' | head -n 1)
          echo MY_ENV_VAR as value: $MY_ENV_VAR
          export MAVEN_HOME=$(find /usr/share -maxdepth 1 -type d -name 'apache-maven-*' | head -n 1)
          java -jar ./target/com.dummy.examples-1.0-SNAPSHOT.jar

      # Defines tags to be applied to the image, depending on the tag definitions, see: https://github.com/docker/metadata-action
      - name: Docker metadata
        id: metadata
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMG_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=edge,branch=$repo.default_branch
            type=ref,event=branch

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}